import { Callout } from '@/components/lesson/Callout';
import { Check } from '@/components/lesson/Check';

## Make the rubric explicit

A constitution is a collection of principles such as helpfulness, honesty, or non-harm. The model is asked to inspect a candidate answer against a principle and then propose a revision. The process turns a vague alignment target into text that can be shown, debated, and revised.

## Critique is a generated label

The critique itself may be produced by a model, so it is not automatically neutral or correct. Principles can conflict, prompts can be ambiguous, and a model can learn to recite the rubric without changing its behavior. The method changes where supervision enters; it does not remove the need for evaluation.

## Separate policy from proof

Use the draft/revised columns and the principle-strength slider to compare an unreviewed response with a critique-and-revision path. Ask which failure modes the principle addresses and which remain outside its wording. A constitution is a steering instrument, not a formal guarantee of safety.

$$
\text{response} \rightarrow \text{critique} \rightarrow \text{revision}
$$

<Callout title="Work the idea" targetInteractive="principle-critique">
Open **Principle-strength critique and revision**. Switch between **draft** and **revised**, then drag **principle strength**. On the revised path, the violation score should fall as the principle gets stronger; helpfulness dips in the middle and recovers. The draft path barely moves — critique without revision does not change the answer.
</Callout>

<Check
  id="constitutional-ai.core-idea"
  question={"What is the purpose of the critique step in Constitutional AI?"}
  options={["To compare a response with an explicit principle before revision.", "To increase the vocabulary size.", "To remove all human-defined behavior.", "To skip evaluation entirely."]}
  answer={0}
  explanation={"The critique step asks whether the candidate response follows the written principle and supplies guidance for a revised answer."}
/>

## Recap

The central idea is **Constitutional AI**: a written principle is used to critique a draft and produce a revision, turning a vague “be good” target into an inspectable loop. Principle strength is the control — too weak and the violation remains; too blunt and helpfulness dips before the revision recovers.
